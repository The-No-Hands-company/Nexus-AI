apiVersion: v1
kind: Service
metadata:
  name: nexus-ai
  namespace: nexus-ai
  labels:
    app: nexus-ai
spec:
  type: ClusterIP
  selector:
    app: nexus-ai
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
