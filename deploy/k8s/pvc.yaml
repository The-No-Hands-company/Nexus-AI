apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nexus-ai-pvc
  namespace: nexus-ai
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: standard   # adjust to your cluster's storage class
