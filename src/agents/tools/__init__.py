"""
src/agents/tools/ — Typed tool modules for specialist agents

Each file in this package is a typed wrapper around the corresponding
tool_* function from src/tools_builtin.py or a new implementation.

Modules:
    web_search.py   — structured web search with citations
    file_ops.py     — file system operations with path safety
    shell.py        — sandboxed shell command execution
    rag_query.py    — RAG query with structured result types
"""
