# Optional RAG extras for semantic/vector features.
# Install only when needed to avoid heavyweight production builds:
#   pip install -r requirements.rag.txt

chromadb>=0.5.0
sentence-transformers>=3.0.0
