name: Red Team Suite

on:
  workflow_dispatch:
  schedule:
    - cron: "20 3 * * *"
  pull_request:
    paths:
      - "src/safety/**"
      - "tests/test_red_team_pipeline.py"
      - ".github/workflows/red-team-suite.yml"

jobs:
  red-team:
    runs-on: ubuntu-latest
    env:
      PYTEST_DISABLE_PLUGIN_AUTOLOAD: "1"
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest

      - name: Run adversarial red-team tests and write summary
        run: |
          set +e
          start_ts=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
          python -m pytest tests/test_red_team_pipeline.py -q
          rc=$?
          end_ts=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
          status="passed"
          if [[ $rc -ne 0 ]]; then
            status="failed"
          fi
          export START_TS="$start_ts"
          export END_TS="$end_ts"
          export STATUS="$status"
          export EXIT_CODE="$rc"
          python - <<'PY'
import json
import os

summary = {
    "workflow": "red-team-suite",
    "status": os.getenv("STATUS", "unknown"),
    "exit_code": int(os.getenv("EXIT_CODE", "1")),
    "start_time_utc": os.getenv("START_TS", ""),
    "end_time_utc": os.getenv("END_TS", ""),
    "run_id": os.getenv("GITHUB_RUN_ID", ""),
    "run_number": os.getenv("GITHUB_RUN_NUMBER", ""),
    "sha": os.getenv("GITHUB_SHA", ""),
    "ref": os.getenv("GITHUB_REF", ""),
    "suite": "tests/test_red_team_pipeline.py",
}

with open("red-team-summary.json", "w", encoding="utf-8") as f:
    json.dump(summary, f, indent=2)
PY
          exit $rc

      - name: Upload red-team JSON summary
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: red-team-summary
          path: red-team-summary.json
